## Context

Replace the current TanStack Start starter home with a single-page educational blog post explaining secure Node.js environment-variable handling in the context of recent software supply-chain attacks. The finished `/` route should be a polished, responsive article whose security guidance and incident references are grounded in current primary sources, while preserving the existing app’s light/dark theme support.

## Approach

1. **Turn the generated starter into one document route.** In `src/routes/index.tsx`, replace `App` with the complete static article component; do not add a CMS, data fetch, dependency, or client-side state. Set the article title to **“Environment variables are not a vault”** and the dek to explain that environment variables are an exposure boundary, not a secrets-management solution. Use semantic `<article>`, `<header>`, `<section>`, `<aside>`, `<figure>`, `<pre><code>`, `<ol>`, and `<footer>` markup so the page remains useful without its visual treatment.
   - Compose the page in this fixed reading order: a small “Security field note” label and `8 min read`; title/dek/byline `Updated July 2026`; a high-contrast “the premise changed” callout; an “attack chain” explainer; practical “use a narrower environment” guidance; an executable-looking, redacted Node configuration-validation example; CI and npm-install guardrails; a five-item incident checklist; and a primary-source references section.
   - Explain the September 2025 Shai-Hulud campaign accurately: compromised npm packages used `postinstall` malware to collect secrets, including environment variables; GitHub reports it was self-replicating and removed 500+ compromised packages. Link these claims inline to GitHub’s September 22, 2025 response and the detailed Wiz incident analysis. Do not reproduce malicious package names, IOCs, payloads, or secret-shaped values.
   - State the security model precisely: every dependency and child process that inherits an environment can read its values; therefore pass a purpose-built allowlist to child processes rather than `process.env`, keep developer `.env` files out of Git and build output, inject production secrets from the deployment platform’s secret manager, and fail startup when required variables are empty or malformed. Make clear that `.env` is a development convenience—not encrypted storage—and that exposure requires credential rotation, not merely source cleanup.
   - Include exactly two short, safe code samples: (a) a TypeScript `loadConfig(env: NodeJS.ProcessEnv)` validator returning `{ databaseUrl, stripeSecretKey }` after checking non-empty values, with errors naming keys but never values; (b) `spawn(command, args, { env: { PATH: process.env.PATH, NODE_ENV: 'production' } })` to contrast with spreading `process.env`. Render both as non-copying presentation code and use placeholders only.
   - Present the operational controls as concrete text: commit and enforce the lockfile with `npm ci`; review install hooks in a quarantined/no-secret environment using `npm ci --ignore-scripts` before enabling required scripts; scope and short-live CI credentials; use npm OIDC trusted publishing instead of a long-lived publish token; enable FIDO/WebAuthn 2FA; rotate tokens and investigate CI/developer hosts immediately after a suspected dependency compromise. Link each externally verifiable recommendation to Node.js or npm documentation.
   - End with a “Do this this week” checklist: inventory injected secret names by environment, remove broad CI secret inheritance, separate package publishing from deployment credentials, enforce deterministic installs, and rehearse detection/rotation. Do not add forms, analytics, social sharing, subscriptions, or a contact CTA.

2. **Make the shell match a single-page publication.** In `src/components/Header.tsx`, retain `ThemeToggle` and replace the generated product/nav/social controls with a compact publication masthead: a home link labelled `WPR / SECURITY NOTES` and three in-document anchors (`Threat model`, `Safer patterns`, `Checklist`) targeting the article section IDs. Remove the `/about` `Link`, TanStack documentation link, and social SVGs. In `src/components/Footer.tsx`, replace generated copyright/social content with a minimal source/disclaimer line: `Practical guidance, not incident-response advice.` and a same-page “Back to top” link. Keep the current-year rendering.
   - Delete `src/routes/about.tsx`; it is a generated starter route with no post role. The only static page route after the clean cutover is `/`; `routeTree.gen.ts` must be regenerated through the existing `bun --bun run generate-routes` script so it contains no `/about` route.

3. **Apply an editorial security visual system without changing the theme model.** In `src/styles.css`, retain the existing CSS variables, `ThemeToggle` contract (`light`, `dark`, `auto`), Google font imports, and Tailwind setup. Replace starter/demo-specific presentation rules (`island-shell`, `feature-card`, `demo-*`, `rise-in`) with article-specific classes used only by the new route and chrome: a narrow reading measure, a full-width hero with a restrained dark “terminal” code treatment, numbered attack-chain cards, a warning callout, prose heading/list/link styles, source cards, and responsive anchor navigation.
   - Keep all colors expressed through the existing semantic custom properties or new `--article-*` variables defined in both light and dark theme blocks. Preserve sufficient text contrast; no fixed light background may be paired with a dark-theme text variable. At `max-width: 640px`, stack the masthead and remove any horizontal-only layout; long code blocks must scroll horizontally instead of overflowing.
   - Use typography, spacing, borders, and CSS-only decorative grid/line motifs—not stock imagery or downloaded assets—to convey the supply-chain/security theme. The existing `Fraunces` display face and `Manrope` body face remain the sole fonts.

4. **Publish accurate metadata and source attribution.** In `src/routes/__root.tsx`, change the existing root `head` title literal from `TanStack Start Starter` to `Environment Variables Are Not a Vault | WPR`; add a meta description matching the article’s subject and a canonical URL of `/`. Keep `THEME_INIT_SCRIPT`, `Header`, `Footer`, `TanStackDevtools`, and `Scripts` unchanged.
   - The article’s source section must link to exactly these primary/current references, opening external links in a new tab with `rel="noreferrer"`: GitHub’s [npm supply-chain response](https://github.blog/security/supply-chain-security/our-plan-for-a-more-secure-npm-supply-chain/); Node.js [Security Best Practices](https://nodejs.org/en/learn/getting-started/security-best-practices) and [Permission Model](https://nodejs.org/api/permissions.html); npm [trusted publishers](https://docs.npmjs.com/trusted-publishers), [CI package access guidance](https://docs.npmjs.com/using-private-packages-in-a-ci-cd-workflow), and [`ignore-scripts` configuration](https://docs.npmjs.com/cli/v11/using-npm/config#ignore-scripts); and Wiz’s [Shai-Hulud incident analysis](https://www.wiz.io/blog/shai-hulud-npm-supply-chain-attack). Label Wiz as incident research, not an official npm authority.

## Critical files & anchors

- `src/routes/index.tsx` — `App`; generated landing page to replace with the complete semantic security article and all content/source links.
- `src/styles.css` — root theme token blocks and generated component utility region; preserve theme resolution while replacing starter styling with article layout and responsive rules.
- `src/components/Header.tsx` — `Header`; reduces multi-route starter navigation to the publication masthead and in-page anchors.
- `src/components/Footer.tsx` — `Footer`; removes irrelevant social controls and adds the article footer/disclaimer.
- `src/routes/__root.tsx` — `Route.head`; supplies the document title, description, and canonical metadata without disturbing the theme bootstrap.

## Verification

1. From `/home/billy/projects/wpr`, run `bun --bun run generate-routes`; confirm it completes and generated routing no longer contains the `/about` route.
2. Run `bun --bun run build`; it must produce client and server bundles without TypeScript, route-generation, or Vite errors.
3. Run `bun --bun run dev`, load `http://localhost:3000`, and verify the page title is `Environment Variables Are Not a Vault | WPR`. At desktop width, confirm the masthead anchors scroll to the sections with IDs `threat-model`, `safer-patterns`, and `checklist`; confirm there is no About navigation or starter-card content.
4. At a 390 px-wide viewport, verify the masthead and checklist stack, code samples horizontally scroll inside their containers, and no content causes horizontal page overflow. Toggle Light, Dark, and Auto through `ThemeToggle`; each state must keep article text, code, warning, and source links legible.
5. Open every source-section anchor and confirm it targets the intended source URL. Read the rendered guidance to confirm it includes the two safe code samples, deterministic-install/`--ignore-scripts` qualification, OIDC trusted publishing, incident rotation guidance, and no literal secret/token/IOC values.

## Assumptions & contingencies

- The post is an unsigned WPR editorial note, dated `Updated July 2026`; no author identity or organization branding was supplied.
- The deployment’s secret manager is intentionally provider-neutral. The copy will say “deployment platform secret manager” rather than prescribe a vendor.
- Node’s Permission Model is optional defense-in-depth, not a claim that it stops malicious code. Its linked documentation explicitly says Node trusts code it is asked to run; the article will frame permissions as blast-radius reduction.
- If the generated route tree is not regenerated automatically by Vite after deleting `about.tsx`, run the existing `generate-routes` script before building; do not hand-edit `src/routeTree.gen.ts`.
